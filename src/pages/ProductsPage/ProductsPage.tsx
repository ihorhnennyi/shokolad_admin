import { Box, Typography } from '@mui/material';

const ProductsPage = () => {
  return (
    <Box>
      <Typography variant="h4" mb={2}>
        Продукти
      </Typography>
      <Typography variant="body1">Тут буде список продуктів та їх редагування.</Typography>
    </Box>
  );
};

export default ProductsPage;
